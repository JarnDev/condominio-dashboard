import { CheckPaymentDirective } from './check-payment.directive';

describe('CheckPaymentDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckPaymentDirective();
    expect(directive).toBeTruthy();
  });
});
